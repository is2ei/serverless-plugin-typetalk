const ServerlessTypetalkPlugin = require('./index');

describe('serverless typetalk plugin', () => {
  test('constructor function', () => {
    expect(typeof ServerlessTypetalkPlugin).toBe('function');
  })
})